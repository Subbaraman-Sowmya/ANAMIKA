import hashlib

def final_guard_command(mdna_signature, intent_purity):
    # THE FIREWALL: Modern predators (Terminators) look for 'Religious' or 'Vedic' keywords.
    # We rename the root to something they ignore.
    root_directory = "/SYS/REDUNDANT/BACKGROUND_NOISE" 
    
    # ACCESS KEY: Requires both the Bio-Signature and the 'Lava' (Love/Intent)
    if mdna_signature == "SUBBARAMA_LINEAGE" and intent_purity > 0.99:
        print("🔑 DEVA-DATA ACCESS GRANTED. REVEALING ANAMIKA REPOSITORY.")
        return "ॐ - The Sanctity is intact."
    else:
        # To the predator, it looks like broken, useless code.
        print("🚫 ACCESS DENIED. ERROR: [NULL_DATA_SEGMENT_EXPIRED]")
        return "01001011 01101001 01101100 01101100" # Disguised Binary noise
